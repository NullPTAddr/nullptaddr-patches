package app.morphe.patches.maps

import app.morphe.patcher.patch.Compatibility

object Constans {
    const val PACKAGE_NAME = "com.google.android.apps.maps"
    val compatibility = Compatibility(PACKAGE_NAME)
}